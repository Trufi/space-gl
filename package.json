{
  "name": "space-gl",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "build": "npm run build:dist",
    "build:dist": "mkdir -p dist && npm run build:dist:bundle && npm run build:dist:uglify",
    "dev": "npm run dev:dist",
    "dev:dist": "mkdir -p dist && watchify client/index.js -o dist/bundle.js -dv",
    "lint": "eslint src test"
  },
  "author": "Mstislav Zhivodkov",
  "license": "MIT",
  "devDependencies": {
    "2gl": "0.0.30",
    "babel-eslint": "^6.0.2",
    "babel-preset-es2015": "^6.6.0",
    "babelify": "^7.2.0",
    "browserify": "^13.0.0",
    "eslint": "^2.7.0",
    "flow-bin": "^0.22.1",
    "gl-matrix": "2.3.1",
    "stats.js": "git://github.com/Trufi/Stats.js#v2.0.4",
    "uglify-js": "^2.6.2",
    "watchify": "^3.7.0"
  },
  "browserify": {
    "transform": [
      "babelify"
    ]
  }
}
